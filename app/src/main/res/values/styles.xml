<resources>
    <!-- Base application theme. -->
    <style name="ParentOfAppTheme" parent="Theme.AppCompat.Light" >
        <!-- definitions fo v<21 -->
        <item name="colorPrimary">@color/themeColor</item>
        <item name="android:textColorPrimary">#FFFFFF</item>
    </style>

    <style name="AppTheme" parent="ParentOfAppTheme" >
        <!-- definitions for both v<21 and v>=21 -->
    </style>
</resources>
